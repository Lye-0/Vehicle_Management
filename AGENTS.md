# Vehicle Management 開発ガイド

このリポジトリは、車両顧客管理システムのモノレポです。
`pnpm` workspace で管理し、フロントエンド、API、共有データベーススキーマ、ドキュメント、開発ツールを含みます。

## プロジェクト構造

- `apps/web`：フロントエンド。React、Vite、TypeScript、Firebase Authentication クライアント、Cloudflare Workers API との通信、PDF 帳票生成、印刷。
- `apps/api`：バックエンド。Cloudflare Workers、D1、Drizzle ORM。`apps/api/AGENTS.md` に固有のルールがあります。
- `packages/database`：共有 Drizzle スキーマ。`@vehicle-management/database` として API から参照されます。
- `docs`：開発環境構築、開発・本番環境の分離に関するガイド。
- `tools`：開発用シード、バックアップ、マイグレーション支援スクリプト。
- 各領域の下位ディレクトリには `AGENTS.md` があり、その領域固有の追加ルールを定義します。

## 作業の基本方針

- 実装前に、既存コード、設定ファイル、関連ドキュメントを確認してください。
- 指示されていない大規模なリファクタリングは行わないでください。
- 指示されていない仕様、文言、UI、配置、配色を変更しないでください。
- 関係のないファイルを変更しないでください。
- 既存の設計、命名、構成を優先してください。
- 不明点は推測だけで実装せず、ユーザーに確認してください。
- ユーザーが作成した未コミット変更を破棄、上書き、巻き戻ししないでください。
- 秘密情報をソースコード、ログ、回答、コミットに含めないでください。
- 本番環境で登録している既存ユーザーのデータや互換性を考慮した修正、実装を行ってください。

## 危険な操作

以下は、ユーザーの明示的な許可なしに実行しないでください。

- 本番環境へのデプロイ
- 本番データベースへの変更
- 本番マイグレーション
- データ削除
- データベース初期化
- 破壊的な Git 操作（`reset`、`clean`、`force push` など）
- 大量ファイルの削除
- 秘密情報や認証設定の変更

## パッケージ管理

- このリポジトリでは `pnpm` を使用します。
- `npm` や `yarn` を不用意に混在させないでください。
- `package.json` に `packageManager: "pnpm@11.16.0"` が指定されています。
- 依存関係を追加する前に、既存の依存関係で実現できないか確認してください。
- 依存関係の追加・更新理由をユーザーに報告してください。

## Gitとコミット

- 一つの依頼を進める途中でも、意味のある自己完結した作業単位で適宜コミットしてよいです。
- 小さな編集のたびに機械的にコミットする必要はありません。
- コミット前に `git diff` と `git status` を確認してください。
- 現在の依頼と関係のない変更をコミットに含めないでください。
- ユーザーの既存変更を勝手にコミットしないでください。
- `amend`、`rebase`、`reset`、`revert`、`force push` は明示的な許可なしに行わないでください。
- コミットメッセージは日本語の Conventional Commits 形式にしてください。

```text
<type>(任意のscope): 日本語の説明
```

例：

- `feat(web): 顧客検索機能を追加`
- `fix(api): 売上金額の計算処理を修正`
- `refactor(database): スキーマ定義を整理`
- `test(api): 整備金額のテストを追加`
- `docs: 開発ルールを追加`
- `chore: 依存関係を更新`

`type` や `scope` は変更内容に合ったものを選んでください。

## 検証方針

- トークン消費を抑えるため、検証は CLI コマンドとその出力を優先してください。
- 変更内容に応じて、型チェック、lint、テスト、ビルドなどを使用してください。
- 実際に `package.json` 等で確認できるコマンドだけを記載・実行してください。
- 成功した検証と失敗した検証を区別して報告してください。
- 失敗を無視したり、未実行の検証を成功したと表現したりしないでください。
- ブラウザ操作ツール、ブラウザ自動化、画面操作スキル、スクリーンショットによる検証は、ユーザーが明示的に依頼しない限り使用しないでください。
- UI 変更でも、まず CLI で可能な型チェック、lint、テスト、ビルドを実行してください。
- CLI だけでは視覚的な正しさを保証できない場合は、その限界を明記してください。
- 開発サーバーを起動する必要がある場合も、可能な限りログや HTTP レスポンスなど CLI で確認してください。

### よく使う検証コマンド

```powershell
# 型チェック・ビルド（Web）
pnpm --filter web build

# Lint（Web）
pnpm --filter web lint

# テスト（API）
pnpm --filter api test --run

# ローカル開発環境全体起動
pnpm dev
```

## 環境と秘密情報

- `.env`、`.env.*`、`.dev.vars`、`.dev.vars.*` には秘密情報を含む可能性があります。
- これらのファイルの値を読み取ったり、回答やコードに出力したりしないでください。
- 環境変数の構成は `README.md`、`docs/environment-separation.md`、`docs/local-development.md` を参照してください。
- 例ファイルや型定義、設定コード、環境変数名のみで構成を把握してください。
- 秘密鍵、トークン、パスワード、実際の接続情報は表示しないでください。
